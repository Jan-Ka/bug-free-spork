import { RechnungBetragNettoPipe } from './rechnung-betrag-netto.pipe';

describe('RechnungBetragNettoPipe', () => {
  it('create an instance', () => {
    const pipe = new RechnungBetragNettoPipe();
    expect(pipe).toBeTruthy();
  });
});
