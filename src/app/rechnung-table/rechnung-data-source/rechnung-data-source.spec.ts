import { RechnungDataSource } from './rechnung-data-source';

describe('RechnungDataSource', () => {
  it('should create an instance', () => {
    expect(new RechnungDataSource(null)).toBeTruthy();
  });
});
