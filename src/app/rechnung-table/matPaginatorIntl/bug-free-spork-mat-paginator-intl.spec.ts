import { BugFreeSporkMatPaginatorIntl } from './bug-free-spork-mat-paginator-intl';

describe('BugFreeSporkMatPaginatorIntl', () => {
  it('creates an instance', () => {
    expect(new BugFreeSporkMatPaginatorIntl()).toBeTruthy();
  });
});
