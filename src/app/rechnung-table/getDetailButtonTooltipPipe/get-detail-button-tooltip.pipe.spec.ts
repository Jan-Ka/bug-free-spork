import { GetDetailButtonTooltipPipe } from './get-detail-button-tooltip.pipe';

describe('GetDetailButtonTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDetailButtonTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
